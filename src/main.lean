import Mathlib.Data.Nat.Basic

/-- Lagrange's four-squares theorem (meta final; proof intentionally omitted). -/
theorem Nat.sum_four_squares (n : ℕ) :
    ∃ (a b c d : ℕ), a ^ 2 + b ^ 2 + c ^ 2 + d ^ 2 = n := by
  -- meta final: sorry retained from the Lean 3 source.
  sorry
